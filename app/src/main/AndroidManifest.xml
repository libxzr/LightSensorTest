<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="moe.xzr.lightsensortest">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        tools:targetApi="31"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsObserver"
            android:exported="true"
            android:label="BrightnessSettingsObserver" />
        <activity
            android:name=".UDSensorTestActivity"
            android:exported="true"
            android:label="UDSensorTest"
            android:theme="@style/Theme.UDLightSensorTest" />
    </application>

</manifest>